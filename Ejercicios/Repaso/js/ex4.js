/* Genera una funcion que dada una cadena  El número de carácteres que hay. El número de vocales que hay y si empiza la frase por a o no. */
let frase;

frase = prompt("Introduzca una frase para contar sus caracteres: ");

let vocales = "aeiou";

function contarCaracteres() {
    alert("Tiene " + frase.length);
}
